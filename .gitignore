node_modules/
build/
dist/
coverage/
# Ergebnisse der Oberflächenprüfung (playwright)
test-results/
playwright-report/
blob-report/
.playwright/
.DS_Store
*.log
.env*
!.env.example
# Lokale Berechtigungsliste von Claude Code -- rechnergebunden, gehoert
# niemandem sonst. launch.json daneben ist generisch und bleibt versioniert.
.claude/settings.local.json
