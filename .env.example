# Diese App benoetigt KEINE Umgebungsvariablen oder API-Schluessel.
# Alle Daten bleiben lokal auf dem Geraet (DSGVO-konform, serverlos).

# Optional: Port fuer den Auslieferungs-Server (Standard: 3000)
# PORT=3000
